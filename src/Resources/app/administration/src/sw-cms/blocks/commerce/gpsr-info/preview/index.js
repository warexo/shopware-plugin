import template from './sw-cms-preview-gpsr-info.html.twig';
import './sw-cms-preview-gpsr-info.scss';

const { Component } = Shopware;

/**
 * @private since v6.5.0
 * @package content
 */
Component.register('sw-cms-preview-gpsr-info', {
    template,
});